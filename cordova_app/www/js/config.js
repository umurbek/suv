// Cordova template config. Edit API_BASE to your server domain.
// Set to your development server on the same LAN so device can reach it.
const API_BASE = "http://192.168.241.174:8000/";

// Expose globally for index.html (unused when loading live site directly)
window.API_BASE = API_BASE;
